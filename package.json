{
  "name": "badawifour.com",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm run test",
    "dev": "node scripts/dev-server.mjs",
    "deploy": "wrangler deploy",
    "audit:prod": "node scripts/lighthouse-audit.mjs"
  },
  "devDependencies": {
    "wrangler": "4.135.0",
    "lighthouse": "13.5.0"
  },
  "engines": {
    "node": ">=24"
  }
}
